# Cron Job: cron-output-cleanup

**Job ID:** f1d15df14615
**Run Time:** 2026-06-14 04:01:00
**Schedule:** 0 4 * * 0

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Clean up old Hermes Agent cron job output files. Trim cron output directories older than 7 days: find ~/.hermes/cron/output/ -mindepth 1 -maxdepth 1 -type d -mtime +7 -exec rm -rf {} \;
Then check total size remaining: du -sh ~/.hermes/cron/output/
Report the result: how many directories were cleaned, how much space recovered, and the current total size.

## Response

**Cron Output Cleanup Complete**

- **Cleanup command executed successfully** (exit code 0)
- **Current total size**: 46M (`~/.hermes/cron/output/`)

The `find ... -exec rm -rf` command doesn't emit a summary of what was removed, so I can't report the exact count of directories deleted or bytes recovered. The directory is now 46M total.
